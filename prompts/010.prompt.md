---
chatSessionId: 1db50924-1d1a-4790-b60f-29619e7c473c
---
目標：先做一個 minimal demo，先只做 core 的部分，不需要實際的 server，先用 terminal 做 test
- 流程
    - client request send prompt
    - ws server use playwright to open a headed chrome (if not opened), browse to the ai assistant page
    - if not login, ask user to login (server response)
    - if login, select the input box, fill the prompt, and send
    - monitor model state, extract conversation regularly, emit chat event to client  


Create a plan, do not implement yet
